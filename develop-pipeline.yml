trigger:
  - develop

pool:
  name: 'TestAgent'

resources:
  repositories:
    - repository: templates
      endpoint: quangpham1019 (1)
      type: github
      name: quangpham1019/pipeline-repo

extends:
  template: general-pipeline-template.yml@templates
  parameters:
    ciSteps:
      - script: echo Running develop buildSteps from $(Build.SourceBranchName)
      - script: echo Finishing develop buildSteps from $(Build.SourceBranchName)